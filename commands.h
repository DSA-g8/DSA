#include <iostream>
#include <string>
using namespace std;

void runCommand1(string algorithm_name, string input_file, string output_param);
void runCommand2(string algorithm_name, int input_size, string input_order, string output_param);
